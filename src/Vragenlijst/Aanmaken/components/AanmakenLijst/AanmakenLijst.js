import classes from './AanmakenLijst.module.css';

import Card from '../UI/Card';

const AanmakenLijst = () => {
	return (
		<Card className={classes.card}>
			<h2 className={classes.titel}>Vraag toevoegen</h2>
		</Card>
	);
};

export default AanmakenLijst;
